const nombre = "Gemma";
const apellido = "Batlle";

const obj = {
    nom: nombre,
    apell: apellido
}

const fecha = new Date();
const fecha1 = fecha.getMinutes() + 2;
// sessionStorage.setItem("objeto", JSON.stringify(obj));
// localStorage.setItem("objeto", JSON.stringify(obj));

// document.cookie = "objCookie=" + JSON.stringify(obj); "expires=" + JSON.stringify(fecha1);

